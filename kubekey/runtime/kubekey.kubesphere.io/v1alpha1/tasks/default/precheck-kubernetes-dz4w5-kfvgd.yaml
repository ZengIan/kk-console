apiVersion: kubekey.kubesphere.io/v1alpha1
kind: Task
metadata:
  annotations:
    kubesphere.io/rel-path: roles/native/root
  creationTimestamp: "2026-05-17T18:21:33Z"
  generateName: precheck-kubernetes-dz4w5-
  managedFields:
  - apiVersion: kubekey.kubesphere.io/v1alpha1
    fieldsType: FieldsV1
    fieldsV1:
      f:metadata:
        f:annotations:
          .: {}
          f:kubesphere.io/rel-path: {}
        f:generateName: {}
        f:ownerReferences: {}
      f:spec:
        f:hosts: {}
        f:module:
          f:args: {}
          f:name: {}
        f:name: {}
        f:when: {}
    manager: kk
    operation: Update
    time: "2026-05-17T18:21:33Z"
  - apiVersion: kubekey.kubesphere.io/v1alpha1
    fieldsType: FieldsV1
    fieldsV1:
      f:status:
        f:hostResults: {}
        f:phase: {}
    manager: kk
    operation: Update
    subresource: status
    time: "2026-05-17T18:21:34Z"
  name: precheck-kubernetes-dz4w5-kfvgd
  namespace: default
  ownerReferences:
  - apiVersion: kubekey.kubesphere.io/v1
    blockOwnerDeletion: true
    controller: true
    kind: Playbook
    name: precheck-kubernetes-dz4w5
    uid: 688de8ba-ce52-4c3f-80d6-f68437b46eeb
  resourceVersion: "3"
  uid: 01f8267a-fcce-4032-a969-a79a40618d85
spec:
  DelegateTo: ""
  hosts:
  - 47-253-179-114
  loop: null
  module:
    args: |-
      ADD_PATHS="/usr/local/bin"
      BACKUP_FILE="/etc/sudoers.backup.$(date +%Y%m%d_%H%M%S)"
      cp -p /etc/sudoers "$BACKUP_FILE"
      TMP_FILE=$(mktemp /tmp/sudoers_update.XXXXXX)
      chmod 600 "$TMP_FILE"
      cp -p /etc/sudoers "$TMP_FILE"
      cleanup() {
          rm -f "$TMP_FILE"
          rm -f "$BACKUP_FILE"
      }
      trap cleanup EXIT INT TERM
      if grep -q "^Defaults.*secure_path" "$TMP_FILE"; then
          echo "find current secure_path 配置"
          EXISTING_LINE=$(grep "^Defaults.*secure_path" "$TMP_FILE")
          EXISTING_PATH=$(echo "$EXISTING_LINE" | sed -e 's/.*secure_path[[:space:]]*=[[:space:]]*"\{0,1\}\([^"[:space:]]*\)"\{0,1\}.*/\1/')
          if [ -n "$EXISTING_PATH" ]; then
              echo "current secure_path: $EXISTING_PATH"
              NEW_PATH="$EXISTING_PATH"
              IFS=':' read -ra PATHS_TO_ADD <<< "$ADD_PATHS"
              for path in "${PATHS_TO_ADD[@]}"; do
                  if [[ ":$NEW_PATH:" != *":$path:"* ]]; then
                      NEW_PATH="$NEW_PATH:$path"
                  fi
              done
              if [ "$NEW_PATH" != "$EXISTING_PATH" ]; then
                  echo "new secure_path: $NEW_PATH"
                  sed -i "s/^Defaults.*secure_path/# &/" "$TMP_FILE"
                  echo "Defaults secure_path=\"$NEW_PATH\"" >> "$TMP_FILE"
              fi
          else
              echo "warning: can not get current secure_path"
              echo "Defaults secure_path=\"$ADD_PATHS\"" >> "$TMP_FILE"
          fi
      else
          echo "current secure_path config not found,set new data"
          echo "Defaults secure_path=\"$ADD_PATHS\"" >> "$TMP_FILE"
      fi
      if /usr/sbin/visudo -cf "$TMP_FILE" > /dev/null 2>&1; then
          cp -f "$TMP_FILE" /etc/sudoers
          chmod 440 /etc/sudoers
          echo "already update /etc/sudoers"
          echo "after update secure_path config:"
          grep "^Defaults.*secure_path" /etc/sudoers
      else
          echo "error: something went wrong,roll back"
          echo "please check visudo log:"
          /usr/sbin/visudo -cf "$TMP_FILE"
          cp -f "$BACKUP_FILE" /etc/sudoers
          chmod 440 /etc/sudoers
          echo "already roll back"
          exit 1
      fi
      echo "success"
    name: command
  name: OS | Add sudo secure path
  when:
  - '{{.os.type | eq "Linux"}}'
status:
  hostResults:
  - error: module run failed
    host: 47-253-179-114
    loopResults:
    - error: 'failed to execute template ''{{.os.type | eq "Linux"}}'': template:
        kubekey:1:5: executing "kubekey" at <.os.type>: can''t evaluate field type
        in type interface {}'
      item: null
      stdout: failed
  phase: Failed
